{
  "name": "crc20",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ lunc/crc20",
    "create-local": "graph create --node http://localhost:8020/ lunc/crc20",
    "remove-local": "graph remove --node http://localhost:8020/ lunc/crc20",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 lunc/crc20"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
